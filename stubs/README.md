# Stubs

Placeholder integration plugins — not production-ready, **not** listed in [`registry.json`](../registry.json).

| Plugin | Scope | Status |
|--------|-------|--------|
| [`foundry-vtt-sync`](foundry-vtt-sync/) | global | Integration stub (`network:fetch`) |

Install locally via `pnpm run plugins:link` from `esiana-core`, or Admin → **Install from URL**. Stubs are not cataloged until promoted to `registry.json` with a pinned SHA.
